export type TypeProps = {
    
}
export interface DataType {
    code: string;
    _id: string;
    name: string;
    package: string;
    manufacturer: string;
    country: string;
    unit:string;
    note: string,
    status: string,
  }